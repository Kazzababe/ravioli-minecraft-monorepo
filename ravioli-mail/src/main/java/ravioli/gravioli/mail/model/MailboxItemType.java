package ravioli.gravioli.mail.model;

import ravioli.gravioli.mail.item.MailboxItem;

/**
 * {@link MailboxItem MailboxItems} each have a different type which determines how they're parsed
 * both from a configuration source and any external storage points.
 */
public enum MailboxItemType {
    ITEM;
}
